import { colors } from "./colors";
import { spacing, lineHeights } from "./colors";
import { sizes } from "./sizes";
import { fonts, fontSizes, fontWeights } from "./fonts";

const theme = {
  colors,
  spacing,
  lineHeights,
  sizes,
  fonts,
  fontSizes,
  fontWeights,
};

export default theme;
